{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "watch:ts": "tsc -w",
    "watch:css": "sass --watch src:dist",
    "build": "tsc && sass src:dist --style compressed"
  },
  "type": "module",
  "devDependencies": {
    "sass": "^1.100.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.8"
  }
}
